{
  "_note": "B12 ratchet: total non-test Go LOC ceiling. Raising this is a conscious act reviewed in the PR that needs it. Measured 29745 at 2026-09-07 (+118 over 29627: the v0.40.5 specialist-review round — the -requarantine selector gains the two bounds the council found missing (the signature must come from the `git apply` STAGE, and the row must carry no diff provenance, i.e. predate v0.40.4), each counted and printed as a boundary line so a refusal is visible rather than inferred from a smaller number; an undecodable-line count; a three-state report mode so an ABORTED apply can never print the same header as a completed one; the pre-state backup removed on every abort path so a .bak beside the oracle always means the rewrite happened, and refused when one already exists at the same stamp; the pre-rename guard comparing the oracle BY IDENTITY (a byte-for-byte re-read) rather than size+mtime, which no stat ordering can dodge and which a test seam can prove red; a warnOut seam and a branched message for removeWorktree. Live match set unchanged at 22 rows — all 22 carry `verify-fail: git apply:` and no diff_source). PRIOR: B12 ratchet: total non-test Go LOC ceiling. Raising this is a conscious act reviewed in the PR that needs it. Measured 29627 at 2026-09-07 (+364: v0.40.5 mr-goldreplay -requarantine — the operator-run quarantine of the rows the pre-0.40.4 capture defect poisoned: union selector over every evidence class, per-lane before/after report, dry run by default, -apply gated on a committed-clean oracle with a backup, a re-stat abort and a byte-splice stamp; policyeval.IsEvidence takes the quarantined marker so goldreplay, mr-scorecard and the B15 canary share one hole definition; loadDone keeps a quarantined row's identity in the drift indexes so a refill at the same pin is not a re-key). PRIOR: B12 ratchet: total non-test Go LOC ceiling. Raising this is a conscious act reviewed in the PR that needs it. Measured 29263 at 2026-09-07 (+334: v0.40.4 mr-goldreplay candidate-diff capture repair — gitOut (stdout/stderr split, timeout honoured) replacing the CombinedOutput gitC whose git stderr became patch bytes; captureWorktreeDiff with the build-cache deny-list pathspec and hole-on-failure; patchGrammarViolation pre-apply gate; Row.diff_source/diff_truncated_lines provenance (absent = unknown); the worktree-vs-printed apply-failure asymmetry in applyVerifyOutcome; per-cell nonce on the worktree and candidate-diff paths, full-stderr worktree notes, removeWorktree with a RemoveAll fallback; +75 from the specialist review round: gitOut WaitDelay + mingw64 launcher substitution so the timeout reaches the process holding the pipes, env scrub on the git spawn (B13), error-first hole notes, a warning on every failed worktree remove, a substantive-lines truncation receipt, rune-safe bounded notes with a cut marker, the shared goldtask.StageGitApply label — after the 2026-09-06 astra/luna sweeps recorded the harness's own corrupt patches as model failures on every lane that edits in place). PRIOR: B12 ratchet: total non-test Go LOC ceiling. Raising this is a conscious act reviewed in the PR that needs it. Measured 28929 at 2026-09-06 (+15: v0.40.3 freelane.cleanToken — a UTF-8 BOM in a hand-created credential file is not whitespace, so it rode into the Authorization header and the vendor answered 401 with a valid key; proven live on the same file: 0.40.2 auth_error, 0.40.3 correct answer). PRIOR: B12 ratchet: total non-test Go LOC ceiling. Raising this is a conscious act reviewed in the PR that needs it. Measured 28914 at 2026-09-06 (+51: v0.40.2 codexlane.EnsureHomeWith seeding the native Windows sandbox mode into the per-run CODEX_HOME with a validated two-mode set, orchcfg codex_windows_sandbox, and the three callers — after Codex CLI 0.153.4 rejected every shell command on Windows in the hermetic home and the first Astra sweep recorded harness failures as ok rows). PRIOR: B12 ratchet: total non-test Go LOC ceiling. Raising this is a conscious act reviewed in the PR that needs it. Measured 28863 at 2026-09-06 (+158: v0.40.1 codex plan facts — quotapoll.CodexFacts/PollCodexFactsAt decoding plan_type, model_usage and additional_rate_limits from the live prolite capture with windowSnapshots shared by the main and per-model limits; poll-state recording of the default-subject facts (recordCodexFacts, outage-preserving) and the status codex_plan block; mr-goldreplay probeBudgets/probeBudgetHold generalised to a per-lane table so -codex-budget-pct caps a roster sweep on the weekly window the way -copilot-budget-pct caps the month). PRIOR: B12 ratchet: total non-test Go LOC ceiling. Raising this is a conscious act reviewed in the PR that needs it. Measured 28705 at 2026-09-06 (+1347: v0.40.0 free-provider lanes (W4) — internal/orch/freelane (data-shaped five-provider registry that is the exclusion list, credential files, OpenAI chat body/parse with the reasoning-echo rule, guarded HTTP run, Groq header/duration parsing, Cloudflare neuron meter), ledger WinDay/WinTrial + NextDailyReset, orchcfg free_* fields and lookups, five run<Lane>Lane wrappers that each reach egress.Plan (B14) over one runFreeLane (off → egress → gemini class gate → credential → admission → dry-run → local RPM window → dispatch → ledger → receipt → breaker), applyFreeOutcome and freeLaneStates, the --exclude free group, mr-hook hint windows, two policy-watch doc hashes, and mr-goldreplay per-lane pins + OpenAI-body decoder + cwd-less free dispatch; +22 from the review round: an explicit case \"gemini\" so the B14 canary sees the gated lane, and a trial-window roll that keeps the lifetime credit count. Five lanes for the price of one adapter; each lane is its own router lane by design so a provider outage never masks another.) PRIOR: B12 ratchet: total non-test Go LOC ceiling. Raising this is a conscious act reviewed in the PR that needs it. Measured 27358 at 2026-09-06 (+223: v0.39.0 copilot-review — the quota-gated Copilot code-review request (laneGate skip with a typed exit-0 result and deferred receipt, the in-binary account-separation refusal, gh run on the minted token only, the copilot_review_credits estimate metered on the month window) after the operator asked that a review be skipped when the month is exhausted). PRIOR: B12 ratchet: total non-test Go LOC ceiling. Raising this is a conscious act reviewed in the PR that needs it. Measured 27135 at 2026-09-06 (+244: v0.38.0 copilot AI-credits recalibration — quotapoll.PollCopilot (copilot_internal/user: the month's entitlement/used/reset as a poll-sourced WinMonth snapshot plus CopilotFacts carrying the entitlement as MEASURED capacity, typed absences incl. unlimited and the legacy request unit), pollFetch.pollCopilot + subjectFetch.CapMilli so the poll sets the ledger cap, orchcfg copilot_monthly_credits/copilot_max_ai_credits/copilot_usage_poll with the CLI floor clamp, and the --max-ai-credits per-dispatch spend bound in copilotlane.BuildArgs — after the billing report proved the v0.37 meter counted credits against a 300-request cap). PRIOR: B12 ratchet: total non-test Go LOC ceiling. Raising this is a conscious act reviewed in the PR that needs it. Measured 26891 at 2026-09-06 (+66: v0.37.0 copilot economy — copilotMilliRequests (vendor per-dispatch premium figure floored at one) with its receipt fields, orchcfg.CopilotDefaultModel (1x pin replaces the silent `auto`), and mr-goldreplay's probeBudgetHold (-copilot-budget-pct holds copilot cells as resumable holes at the cap) after a 168-cell probe under auto drained the month). PRIOR: B12 ratchet: total non-test Go LOC ceiling. Raising this is a conscious act reviewed in the PR that needs it. Measured 26825 at 2026-09-02 (+63: v0.36.2 — replayOne takes a NAMED result so the deferred served-model annotation reaches the returned row (the first live copilot smoke lost it to an unnamed result; pinned by a fake-orchestrator integration test); mr-goldreplay copilot lane: -copilot-model/-copilot-effort pins gated like every lane, decodeAgentStream decoding copilot assistant.message envelopes (data.content) and recovering the SERVED model for the row note because a `--model auto` pin resolves vendor-side; run's unknown-lane message lists copilot/local). PRIOR: B12 ratchet: total non-test Go LOC ceiling. Raising this is a conscious act reviewed in the PR that needs it. Measured 26762 at 2026-09-02 (+80: v0.36.1 — mr-hook delegate-mode proposal falls back to the poll-fed ledger (ledgerPressurePct, DropMaxAge-bounded) because the VS Code extension never runs statusLine and so never writes a drop; quotapoll.claudeAiOAuthToken (explicit claudeAiOauth.accessToken read; the tolerant walk had been sending an MCP server token to the usage endpoint since late July, 401 on every claude poll) plus the commented measured codex fallback row in the hard-repo seed so --exclude claude no longer defers with GLM retired). PRIOR: B12 ratchet: total non-test Go LOC ceiling. Raising this is a conscious act reviewed in the PR that needs it. Measured 26682 at 2026-09-01 (+250: delegate-mode support (v0.36.0) — cmd/mr-orchestrate/exclude.go (knownLanes closed set, repeatable/csv excludeFlag, parseExclude with typed unknown-lane error shared by CLI and MCP), spendDownReq.Exclude applied in buildRouteDecision after laneStates and before downshift/boost, the router's masked \"excluded\" state, run --exclude threaded into the internal recommendation plus the force-proof lane_excluded refusal before the lane switch (bare run defaults to claude), MCP exclude args + arrProp schema helper on route and run, and cmd/mr-hook/delegateprop.go (read-only proposal: drop read via ParseDrop for a fresh claude figure, session-file armed check with the 8h TTL mirrored from the Node lib, orchcfg DelegateProposePct default 70 / negative disables). PRIOR: B12 ratchet: total non-test Go LOC ceiling. Raising this is a conscious act reviewed in the PR that needs it. Measured 26432 at 2026-09-01 (+704: the copilot lane — internal/orch/copilotlane (args/parse/run/home: argv contract with the permission/session/egress denylist, JSONL parse against a live-captured fixture, per-dispatch token mint + isolated COPILOT_HOME, version gate), ledger.WinMonth + NextMonthlyReset for the calendar allowance, runCopilotLane + applyCopilotOutcome in cmd/mr-orchestrate, the config-level GLM retirement gate (force-proof, re-enablable), the router's masked \"retired\" state, childenv GitHub-token scrubbing, the RS7 copilot policy-watch sources, and the decode-per-event-type parser the live E2E forced (the stream is polymorphic: data.message is a string on some events, an object on model.message). GLM code is RETAINED by design — retirement is config, not deletion, so a returning subscription costs one JSON field. PRIOR: B12 ratchet: total non-test Go LOC ceiling. Raising this is a conscious act reviewed in the PR that needs it. Measured 25715 at 2026-08-19 (W10 lane surfacing: +49 — catalog.KindTools + Root.flat() predicate unifying the three flat-kind gates, mr-hook tool: rendering + trailer, roots reserved-pack guard + KindTools in the kind error message). PRIOR: B12 ratchet: total non-test Go LOC ceiling. Raising this is a conscious act reviewed in the PR that needs it. Measured 25666 at 2026-08-16 (W9-P item 1 through four review rounds + the exit round's two Minors (data-derived eval table width, normalized-identity refusal message); round 4 completed the ApplyRefresh guard's MODEL half; round 3 added the joinErr never-drop-a-cause chain, the ApplyRefresh plan/index spec consistency refusal, and identity-carrying Hybrid/EmbedRerank labels; round 2 added: decide() error propagation, deadline-attribution fold, TplGuard tripwire, refresh.log identity, flag-presence guard, operator-approved program: the per-model embedding template registry internal/embedtpl (+~200: versioned query/doc templates for embeddinggemma + qwen3-embedding, reranker formatting specs, identity parse/resolve with fail-closed unknown-version refusal) and its wiring — model threaded through embed/rerank HTTP calls instead of a hardcoded literal, template version carried in the index identity Model field, mr-index -tpl/-embed-model build flags with refresh preservation, and mr-hook's tpl-mismatch fail-open refusal branch. All template application is opt-in: a raw spec reproduces pre-registry bytes exactly, so production behavior is unchanged until an operator runs `mr-index build -tpl tpl1`.) PRIOR: B12 ratchet: total non-test Go LOC ceiling. Raising this is a conscious act reviewed in the PR that needs it. Measured 25110 at 2026-08-12 (v0.32.0 round-5 delta: the review OF the round-4 repairs confirmed 14 more findings, incl. a MAJOR cluster where the artifact still DOCUMENTED the round-3 exclusion after round 4 turned it into an imputation. Non-test growth is small and all diagnostic: the blank-model sentinel + tier-branched drift messages (an effort-tier hit really re-spends, a model-tier hit appends a first measurement — asserting the re-spend for both was the same over-claim round 3 removed from the old aggregate guard), the frontier imputation warning, and the tuning-frozen zoo resolver's own obsTuning map. Round 6 (the review of round 5) added driftRefusal as an extracted, testable function and the blank-model display helper, after the round-5 sentinel turned out to be PINNABLE and thus a hole in the money guard. PRIOR: B12 ratchet: total non-test Go LOC ceiling. Raising this is a conscious act reviewed in the PR that needs it. Measured 25110 at 2026-08-12 (v0.32.0 round-4 delta: +66 on top of the +282 — the adversarial review OF the round-3 fixes confirmed two MAJOR regressions the fixes themselves introduced, and their repairs are this increment: (1) the frontier's free-unmeasured exclusion broke the ENVELOPE property (oracle-best exceeded the curve's max because a claude-only task's measured evidence left the numerator while its slot stayed in the denominator) — tasks now stay in the sweep, counted; (2) replacing the aggregate resume guard with per-cell effort drift left a MODEL re-key (pin typo = the 204-row incident) completely unguarded — a second model-drift tier now refuses it with -re-measure as the deliberate override; plus loadDone trim symmetry, a tuning-frozen zoo resolver (heldout coverage was steering the winner's-curse test's own selection), deterministic Regret, and the verify-outcome classification extracted for testability.) ",
  "max_go_loc": 29745
}
